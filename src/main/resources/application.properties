spring.application.name=wecureit_be

spring.datasource.url=jdbc:postgresql://wecureit.ca18uqsmuuzk.us-east-1.rds.amazonaws.com:5432/wecureit?sslmode=require
spring.datasource.username=wecureit_db
spring.datasource.password=wecureit_db
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.address=0.0.0.0
server.compression.enabled=false
spring.mvc.async.request-timeout=5000



# JWT configuration (development defaults). For safety, provide a secure secret via environment variable `JWT_SECRET`.
# The property `jwt.secret` will read from the environment variable `JWT_SECRET` when present.
# Use a base64-encoded 32+ byte secret for HS256, e.g.:
#   openssl rand -base64 32
# Example runtime (macOS/zsh):
#   JWT_SECRET=$(openssl rand -base64 32) ./mvnw spring-boot:run
jwt.secret=${JWT_SECRET:ChangeThisToASecureBase64SecretOrSetViaEnv}
jwt.expiration=3600000
#spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres

# Enable debug logging for Spring Security to diagnose authentication/authorization issues
logging.level.org.springframework.security=DEBUG